<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#ff8800" />
    <meta
      name="description"
      content="BiblioNotes - Manage your personal library of books with ratings, reviews, and ISBN tracking"
    />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta
      name="apple-mobile-web-app-status-bar-style"
      content="black-translucent"
    />
    <meta name="apple-mobile-web-app-title" content="BiblioNotes" />
    <link rel="manifest" href="/manifest.json" />
    <link
      rel="icon"
      type="image/png"
      href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 192 192'><rect fill='%23ff8800' width='192' height='192'/><text x='50%' y='50%' font-size='120' font-weight='bold' fill='white' text-anchor='middle' dominant-baseline='central'>📚</text></svg>"
    />
    <link
      rel="apple-touch-icon"
      href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 192 192'><rect fill='%23ff8800' width='192' height='192'/><text x='50%' y='50%' font-size='120' font-weight='bold' fill='white' text-anchor='middle' dominant-baseline='central'>📚</text></svg>"
    />
    <title>BiblioNotes - Welcome</title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        background: url("/bg-home.png") center/cover no-repeat fixed;
        font-family: "Segoe UI", Arial, sans-serif;
        color: #fff;
        min-height: 100vh;
        position: relative;
      }
      header {
        background: rgba(0, 0, 0, 0.7);
        padding: 0.8rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
      }
      .logo {
        font-size: 1.3rem;
        font-weight: bold;
        color: #ff8800;
      }
      nav {
        display: flex;
        gap: 2rem;
      }
      nav a {
        color: #fff;
        text-decoration: none;
        font-size: 0.9rem;
        transition: color 0.3s;
      }
      nav a:hover {
        color: #ff8800;
      }
      .auth-buttons {
        display: flex;
        gap: 0.8rem;
      }
      .auth-buttons a {
        padding: 0.4rem 0.8rem;
        border-radius: 4px;
        text-decoration: none;
        font-size: 0.85rem;
        transition: all 0.3s;
      }
      .login-btn {
        color: #fff;
        border: 1px solid #ff8800;
      }
      .login-btn:hover {
        background: #ff8800;
        color: #fff;
      }
      .signup-btn {
        background: #ff8800;
        color: #fff;
      }
      .signup-btn:hover {
        background: #ff6600;
      }

      .hero {
        display: flex;
        align-items: center;
        justify-content: space-between;
        max-width: 1200px;
        margin: 3rem auto;
        padding: 0 2rem;
        gap: 2rem;
      }
      .hero-content h1 {
        font-size: 2.8rem;
        margin-bottom: 0.8rem;
        color: #fff;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
      }
      .hero-content p {
        font-size: 1rem;
        color: #fff;
        margin-bottom: 1.5rem;
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
      }
      .get-started-btn {
        background: #ff8800;
        color: #fff;
        padding: 0.8rem 2rem;
        border: none;
        border-radius: 4px;
        font-size: 1rem;
        cursor: pointer;
        transition: all 0.3s;
        text-decoration: none;
        display: inline-block;
      }
      .get-started-btn:hover {
        background: #ff6600;
        transform: scale(1.05);
      }

      .hero-image {
        flex: 1;
        text-align: center;
        font-size: 8rem;
      }

      .quick-access {
        background: transparent;
        padding: 2.5rem 2rem;
        margin-top: 2rem;
      }
      .quick-access h2 {
        text-align: center;
        font-size: 1.8rem;
        margin-bottom: 0.8rem;
        color: #ff8800;
      }
      .quick-access p {
        text-align: center;
        color: #ccc;
        margin-bottom: 1.5rem;
        font-size: 0.95rem;
      }
      .cards-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        max-width: 1200px;
        margin: 0 auto;
        gap: 1.2rem;
        padding: 0 2rem;
      }
      .card {
        border-radius: 12px;
        padding: 2rem;
        text-align: center;
        transition: all 0.3s;
        border: 2px solid;
        position: relative;
        overflow: hidden;
      }
      .card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        opacity: 0.1;
        z-index: 0;
      }
      .card > * {
        position: relative;
        z-index: 1;
      }
      /* Card 1 - Books List */
      .card:nth-child(1) {
        background: linear-gradient(
          135deg,
          rgba(66, 165, 245, 0.2) 0%,
          rgba(66, 165, 245, 0.05) 100%
        );
        border-color: #42a5f5;
      }
      .card:nth-child(1):hover {
        background: linear-gradient(
          135deg,
          rgba(66, 165, 245, 0.3) 0%,
          rgba(66, 165, 245, 0.15) 100%
        );
        box-shadow: 0 8px 24px rgba(66, 165, 245, 0.4);
        transform: translateY(-8px);
      }
      .card:nth-child(1) h3 {
        color: #42a5f5;
      }

      /* Card 2 - Add Book */
      .card:nth-child(2) {
        background: linear-gradient(
          135deg,
          rgba(76, 175, 80, 0.2) 0%,
          rgba(76, 175, 80, 0.05) 100%
        );
        border-color: #4caf50;
      }
      .card:nth-child(2):hover {
        background: linear-gradient(
          135deg,
          rgba(76, 175, 80, 0.3) 0%,
          rgba(76, 175, 80, 0.15) 100%
        );
        box-shadow: 0 8px 24px rgba(76, 175, 80, 0.4);
        transform: translateY(-8px);
      }
      .card:nth-child(2) h3 {
        color: #4caf50;
      }

      /* Card 3 - Edit Book */
      .card:nth-child(3) {
        background: linear-gradient(
          135deg,
          rgba(255, 152, 0, 0.2) 0%,
          rgba(255, 152, 0, 0.05) 100%
        );
        border-color: #ff9800;
      }
      .card:nth-child(3):hover {
        background: linear-gradient(
          135deg,
          rgba(255, 152, 0, 0.3) 0%,
          rgba(255, 152, 0, 0.15) 100%
        );
        box-shadow: 0 8px 24px rgba(255, 152, 0, 0.4);
        transform: translateY(-8px);
      }
      .card:nth-child(3) h3 {
        color: #ff9800;
      }

      /* Card 4 - Register */
      .card:nth-child(4) {
        background: linear-gradient(
          135deg,
          rgba(156, 39, 176, 0.2) 0%,
          rgba(156, 39, 176, 0.05) 100%
        );
        border-color: #9c27b0;
      }
      .card:nth-child(4):hover {
        background: linear-gradient(
          135deg,
          rgba(156, 39, 176, 0.3) 0%,
          rgba(156, 39, 176, 0.15) 100%
        );
        box-shadow: 0 8px 24px rgba(156, 39, 176, 0.4);
        transform: translateY(-8px);
      }
      .card:nth-child(4) h3 {
        color: #9c27b0;
      }

      .card-icon {
        font-size: 3.5rem;
        margin-bottom: 1rem;
        display: block;
      }

      .card p {
        color: #e0e0e0;
        font-size: 0.9rem;
        margin-bottom: 1.5rem;
        line-height: 1.5;
      }

      .card-btn {
        background: #ff8800;
        color: #fff;
        padding: 0.6rem 1.5rem;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 0.9rem;
        transition: all 0.3s;
        text-decoration: none;
        display: inline-block;
        font-weight: 600;
      }
      .card-btn:hover {
        background: #ff6600;
        transform: scale(1.05);
      }
      .card-btn.secondary {
        background: #9c27b0;
      }
      .card-btn.secondary:hover {
        background: #7b1fa2;
      }

      @media (max-width: 768px) {
        .hero {
          flex-direction: column;
          margin: 2rem auto;
        }
        .hero-content h1 {
          font-size: 2rem;
        }
        .hero-image {
          font-size: 5rem;
        }
        .cards-container {
          grid-template-columns: 1fr;
          padding: 0 1rem;
        }
      }

      footer {
        background: rgba(0, 0, 0, 0.7);
        padding: 2rem;
        text-align: center;
        margin-top: 3rem;
        border-top: 2px solid #ff8800;
      }

      footer p {
        color: #ccc;
        font-size: 0.9rem;
        margin-bottom: 0.5rem;
      }

      footer .footer-brand {
        color: #ff8800;
        font-weight: bold;
        font-size: 1rem;
      }

      footer .footer-creator {
        color: #ff8800;
        font-weight: 600;
      }
    </style>
  </head>
  <body>
    <header>
      <div class="logo">BiblioNotes</div>
      <nav>
        <a href="/"></a>
        <% if (loggedIn) { %>
        <a href="/books">My Books</a>
        <a href="/books/new">Add Book</a>
        <% } %>
      </nav>
      <div class="auth-buttons">
        <% if (!loggedIn) { %>
        <a href="/login" class="login-btn">Login</a>
        <a href="/register" class="signup-btn">Sign Up</a>
        <% } else { %>
        <span style="margin-right: 1rem; font-size: 0.75rem"
          >👤 <%= username %></span
        >
        <a href="/logout" class="signup-btn">Logout</a>
        <% } %>
      </div>
    </header>

    <div class="hero">
      <div class="hero-content">
        <h1>Welcome to BiblioNotes!</h1>
        <p>Manage Your Personal Library of Books Easily</p>
        <% if (!loggedIn) { %>
        <a href="/register" class="get-started-btn">Get Started</a>
        <% } else { %>
        <a href="/books" class="get-started-btn">View My Books</a>
        <% } %>
      </div>
      <div class="hero-image"></div>
    </div>

    <div class="quick-access">
      <h2>Quick Access</h2>
      <p>Manage your books and account easily with these quick options:</p>
      <div class="cards-container">
        <div class="card">
          <div class="card-icon">≣≡</div>
          <h3>Books List</h3>
          <p>Shows all books for the logged in user.</p>
          <% if (loggedIn) { %>
          <a href="/books" class="card-btn">View Books</a>
          <% } else { %>
          <a href="/login" class="card-btn">Login to View</a>
          <% } %>
        </div>

        <div class="card">
          <div class="card-icon">+</div>
          <h3>Add Book</h3>
          <p>Quickly add a new book to your library.</p>
          <% if (loggedIn) { %>
          <a href="/books/new" class="card-btn">Add Book</a>
          <% } else { %>
          <a href="/login" class="card-btn">Login to Add</a>
          <% } %>
        </div>

        <div class="card">
          <div class="card-icon">✏️</div>
          <h3>Edit Book</h3>
          <p>Edit the details of your existing books.</p>
          <% if (loggedIn) { %>
          <a href="/books" class="card-btn">Manage Books</a>
          <% } else { %>
          <a href="/login" class="card-btn">Login to Edit</a>
          <% } %>
        </div>

        <div class="card">
          <div class="card-icon">👤</div>
          <h3>Register</h3>
          <p>Create a new account to start using the app.</p>
          <% if (!loggedIn) { %>
          <a href="/register" class="card-btn secondary">Sign Up</a>
          <% } else { %>
          <span style="color: #ccc">Already registered</span>
          <% } %>
        </div>
      </div>
    </div>

    <footer>
      <p>
        <span class="footer-brand">📚 BiblioNotes</span> - Your Personal Library
        Manager
      </p>
      <p>Built by <span class="footer-creator">Irfan</span></p>
      <p style="color: #999; font-size: 0.8rem; margin-top: 1rem">
        © 2026 BiblioNotes. All rights reserved.
      </p>
    </footer>

    <script>
      // Register Service Worker for PWA functionality
      if ("serviceWorker" in navigator) {
        window.addEventListener("load", () => {
          navigator.serviceWorker
            .register("/service-worker.js")
            .then((registration) => {
              console.log("Service Worker registered:", registration);
            })
            .catch((error) => {
              console.log("Service Worker registration failed:", error);
            });
        });
      }
    </script>
  </body>
</html>
