<!DOCTYPE html>
<html>
  <head>
    <title>BiblioNotes - Reading Wishlist</title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
        font-family: "Segoe UI", Arial, sans-serif;
        color: #fff;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }
      header {
        background: rgba(0, 0, 0, 0.7);
        padding: 0.8rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
      }
      .logo {
        font-size: 1.3rem;
        font-weight: bold;
        color: #ff8800;
      }
      nav {
        display: flex;
        gap: 2rem;
      }
      nav a {
        color: #fff;
        text-decoration: none;
        font-size: 0.9rem;
        transition: color 0.3s;
      }
      nav a:hover {
        color: #ff8800;
      }
      nav a.active {
        color: #ff8800;
        font-weight: bold;
      }
      .auth-buttons {
        display: flex;
        gap: 0.8rem;
        align-items: center;
      }
      .auth-buttons span {
        font-size: 0.85rem;
      }
      .logout-btn {
        background: #ff8800;
        color: #fff;
        padding: 0.4rem 0.8rem;
        border-radius: 4px;
        text-decoration: none;
        font-size: 0.85rem;
        transition: all 0.3s;
        border: none;
        cursor: pointer;
      }
      .logout-btn:hover {
        background: #ff6600;
      }

      .main-wrapper {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: flex-start;
        margin-top: 2rem;
      }
      .container {
        width: 100%;
        max-width: 800px;
        background: rgba(0, 0, 0, 0.6);
        border-radius: 12px;
        border: 1px solid #ff8800;
        box-shadow: 0 4px 16px rgba(255, 136, 0, 0.2);
        padding: 2rem 2.5rem;
        margin-bottom: 2rem;
      }
      .wishlist-list {
        margin: 0;
        padding: 0;
        list-style: none;
      }
      .wishlist-list li {
        opacity: 0;
        transform: translateY(20px);
        animation: fadeInUp 0.6s forwards;
        animation-delay: var(--delay);
        background: rgba(255, 136, 0, 0.1);
        border: 1px solid #ff8800;
        border-radius: 8px;
        margin-bottom: 1rem;
        padding: 1rem;
        display: flex;
        align-items: center;
        gap: 1.5rem;
      }
      @keyframes fadeInUp {
        to {
          opacity: 1;
          transform: none;
        }
      }
      .wishlist-list img {
        height: 90px;
        border-radius: 4px;
        box-shadow: 0 1px 4px rgba(255, 136, 0, 0.1);
      }
      .item-details {
        flex: 1;
      }
      .item-title {
        font-size: 1.2rem;
        font-weight: bold;
        margin-bottom: 0.3rem;
        color: #ff8800;
      }
      .item-author {
        color: #ccc;
        font-size: 1rem;
        margin-bottom: 0.5rem;
      }
      .item-reason {
        font-size: 0.9rem;
        color: #aaa;
        margin-bottom: 0.5rem;
        font-style: italic;
      }
      .item-meta {
        font-size: 0.85rem;
        color: #888;
      }
      .actions {
        margin-top: 0.8rem;
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
      }
      .btn {
        display: inline-block;
        padding: 0.5rem 1rem;
        background: #ff8800;
        color: #fff;
        border: none;
        border-radius: 4px;
        text-decoration: none;
        font-size: 0.85rem;
        cursor: pointer;
        transition: all 0.3s;
      }
      .btn:hover {
        background: #ff6600;
        transform: scale(1.05);
      }
      .btn-success {
        background: #4caf50;
      }
      .btn-success:hover {
        background: #45a049;
      }
      .delete-btn {
        background: #ff3300;
      }
      .delete-btn:hover {
        background: #cc0000;
      }
      .add-btn {
        display: inline-block;
        padding: 0.6rem 1.5rem;
        background: #ff8800;
        color: #fff;
        border: none;
        border-radius: 4px;
        text-decoration: none;
        font-size: 0.95rem;
        cursor: pointer;
        margin-bottom: 1.5rem;
        transition: all 0.3s;
      }
      .add-btn:hover {
        background: #ff6600;
      }
      .page-title {
        font-size: 1.8rem;
        margin-bottom: 1rem;
        color: #ff8800;
        text-align: center;
      }
      .empty-state {
        text-align: center;
        padding: 3rem 1rem;
        color: #aaa;
      }
      .empty-state p {
        margin-bottom: 1.5rem;
      }
      form {
        display: inline;
      }
      .form-btn {
        padding: 0.5rem 1rem;
        background: #ff3300;
        border: none;
        border-radius: 4px;
        color: #fff;
        cursor: pointer;
        font-size: 0.85rem;
        transition: all 0.3s;
      }
      .form-btn:hover {
        background: #cc0000;
      }
    </style>
  </head>
  <body>
    <header>
      <div class="logo">📚 BiblioNotes</div>
      <nav>
        <a href="/books">My Books</a>
        <a href="/wishlist" class="active">📖 Wishlist</a>
      </nav>
      <div class="auth-buttons">
        <span><%= username %></span>
        <form action="/logout" method="GET" style="display: inline">
          <button type="submit" class="logout-btn">Logout</button>
        </form>
      </div>
    </header>
    <div class="main-wrapper">
      <div class="container">
        <h1 class="page-title">📖 Reading Wishlist</h1>
        <a href="/wishlist/new" class="add-btn">+ Add Book to Wishlist</a>

        <% if (wishlistItems && wishlistItems.length > 0) { %>
        <ul class="wishlist-list">
          <% wishlistItems.forEach((item, index) => { %>
          <li style="--delay: <%= index * 0.1 %>s">
            <% if (item.cover_url) { %>
            <img
              src="<%= item.cover_url %>"
              alt="<%= item.title %>"
              onerror="this.src='/placeholder.jpg'"
            />
            <% } else { %>
            <div
              style="
                width: 90px;
                height: 90px;
                background: #666;
                border-radius: 4px;
                display: flex;
                align-items: center;
                justify-content: center;
              "
            >
              <span style="font-size: 3rem">📕</span>
            </div>
            <% } %>
            <div class="item-details">
              <div class="item-title"><%= item.title %></div>
              <div class="item-author">by <%= item.author %></div>
              <% if (item.reason) { %>
              <div class="item-reason">Why: <%= item.reason %></div>
              <% } %> <% if (item.isbn) { %>
              <div class="item-meta">ISBN: <%= item.isbn %></div>
              <% } %>
              <div class="item-meta">
                Added: <%= new Date(item.added_date).toLocaleDateString() %>
              </div>
              <div class="actions">
                <form
                  action="/wishlist/<%= item.id %>/read"
                  method="POST"
                  style="display: inline"
                >
                  <button type="submit" class="btn btn-success">
                    ✓ Mark as Read
                  </button>
                </form>
                <form
                  action="/wishlist/<%= item.id %>/delete"
                  method="POST"
                  style="display: inline"
                  onsubmit="return confirm('Remove from wishlist?');"
                >
                  <button type="submit" class="form-btn delete-btn">
                    ✕ Remove
                  </button>
                </form>
              </div>
            </div>
          </li>
          <% }); %>
        </ul>
        <% } else { %>
        <div class="empty-state">
          <p>Your wishlist is empty! 📚</p>
          <p>Start adding books you'd like to study in the future.</p>
          <a href="/wishlist/new" class="add-btn">+ Add Book to Wishlist</a>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
